[tox]
envlist = py27
[testenv]
setenv =
    AUDIOPYLE_HOME = {toxinidir}/..
    VAMP_PATH = {toxinidir}/resources/vamp_plugins
sitepackages = False
deps =
    {toxinidir}/../commons
    pytest
recreate = True
commands =
    ./deploy.sh
    py.test
