[tox]
envlist = py27
[testenv]
setenv =
    AUDIOPYLE_HOME = {toxinidir}/..
sitepackages = False
deps =
    {toxinidir}/../commons
    discover
recreate = True
commands =
    ./deploy.sh
    discover -v
